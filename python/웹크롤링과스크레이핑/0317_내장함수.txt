dict() : dic객체 생성
dir() : 현재 스코프 이름의 리스트, 객체의 속성 이름 리스트를 추출한다
filter() 
int() : 정수로 변환
len() : 객체의 길이 추출
list()
map()
max()
min()
open(): 파일을 열고 파일 객체를 반환
print()
repr(): 사람이 읽기 쉬운 문자열로 변환해서 반환
reversed() : 순서를 역순으로
sorted() : 반복 가능한 객체를 정렬한 새로운 리스트로 만든다
str() : 문자열로 변환
type() : 객체의 자료형을 반환한다.




